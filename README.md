# GitLab EE Documentation - Terminal Edition

Статический сайт документации GitLab Enterprise Edition в стиле Linux терминала.

## 🚀 Возможности

- **Терминальный дизайн**: Стилизация под bash терминал с голубым текстом на темном фоне
- **Категории**: Автоматическая группировка документов по категориям
- **Карточки**: Компактное отображение с кратким описанием
- **Модальные окна**: Детальная информация о функциях и полные документы
- **Поиск**: Быстрый поиск по названиям, категориям и содержимому
- **Markdown рендеринг**: Полная поддержка markdown в документах
- **GitLab Shortcodes**: Автоматическая конвертация Hugo shortcodes (tabs, alerts, details, history, icons)

## 📁 Структура проекта

```
gitlab-ee-cl/
├── index.html                          # Главная страница
├── styles.css                          # Терминальные стили
├── script.js                           # Логика приложения
├── gitlab_premium_summaries_ru.md      # Центральный файл с описаниями
└── premium_docs/                       # Папка с полными документами
    ├── administration/
    ├── api/
    ├── ci/
    └── ... (другие категории)
```

## 🌐 Локальный запуск

### Вариант 1: Python HTTP Server

```bash
python3 -m http.server 8000
```

Откройте браузер: `http://localhost:8000`

### Вариант 2: Node.js HTTP Server

```bash
npx http-server -p 8000
```

## 📤 Публикация на GitHub Pages

### Способ 1: Через веб-интерфейс GitHub

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта
3. Перейдите в **Settings** → **Pages**
4. В разделе **Source** выберите:
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`
5. Нажмите **Save**
6. Сайт будет доступен по адресу: `https://ваш-username.github.io/название-репозитория/`

### Способ 2: Через командную строку

```bash
# Инициализируйте git репозиторий (если еще не создан)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: GitLab EE Docs Terminal Edition"

# Добавьте remote репозиторий
git remote add origin https://github.com/ваш-username/название-репозитория.git

# Отправьте на GitHub
git push -u origin main

# Включите GitHub Pages (через веб-интерфейс или CLI)
```

### Способ 3: Автоматический деплой с GitHub Actions

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
```

## 🎨 Кастомизация

### Изменение цветовой схемы

Отредактируйте переменные в `styles.css`:

```css
:root {
    --terminal-bg: #0c0c0c;          /* Цвет фона */
    --terminal-fg: #5ec4ff;          /* Основной цвет текста */
    --terminal-prompt: #4caf50;      /* Цвет промпта $ */
    --terminal-link: #5ec4ff;        /* Цвет ссылок */
    --terminal-highlight: #ffd700;   /* Цвет подсветки */
}
```

### Изменение шрифта

В `styles.css` измените `font-family`:

```css
body {
    font-family: 'Ваш-шрифт', 'Consolas', monospace;
}
```

## 🔧 Технологии

- **HTML5**: Структура страницы
- **CSS3**: Терминальный дизайн с градиентами и анимациями
- **Vanilla JavaScript**: Парсинг, фильтрация, модальные окна
- **Marked.js**: Рендеринг Markdown
- **GitHub Pages**: Хостинг статического сайта

## 📝 Формат данных

Файл `gitlab_premium_summaries_ru.md` имеет структуру:

```markdown
## 📄 category/document.md

**Зачем это нужно**
Краткое описание...

**Основные возможности**
- Функция 1
- Функция 2

**Важные нюансы**
- Нюанс 1
- Нюанс 2

---
```

## 🔄 Поддержка GitLab Shortcodes

Сайт автоматически конвертирует Hugo shortcodes из документации GitLab в красивые HTML-элементы:

### Details блоки
```markdown
{{< details >}}
- Tier: Premium, Ultimate
- Offering: GitLab Self-Managed
{{< /details >}}
```
→ Раскрывающийся блок с заголовком "📋 Детали"

### Alert блоки
```markdown
{{< alert type="note" >}}
Важная информация
{{< /alert >}}
```
→ Стилизованные предупреждения с иконками (📝 note, ⚠️ warning, ℹ️ info, 🚨 danger, 💡 tip)

### Вкладки (Tabs)
```markdown
{{< tabs >}}
{{< tab title="Linux" >}}
Инструкции для Linux
{{< /tab >}}
{{< tab title="macOS" >}}
Инструкции для macOS
{{< /tab >}}
{{< /tabs >}}
```
→ Интерактивные вкладки в терминальном стиле

### История изменений
```markdown
{{< history >}}
- GitLab 16.0: добавлена функция
- GitLab 15.5: улучшения
{{< /history >}}
```
→ Блок с заголовком "📅 История изменений"

### Иконки
```markdown
{{< icon name="star" >}}
```
→ Конвертируются в Unicode символы (⭐, ➕, ✓, и т.д.)

## 🎯 Использование

1. **Выбор категории**: Кликните на категорию в левой панели
2. **Просмотр карточки**: Кликните на карточку для просмотра возможностей и нюансов
3. **Полный документ**: Кликните "подробнее" для открытия полного markdown документа
4. **Поиск**: Введите текст в строку поиска для фильтрации документов

## 📱 Адаптивность

Сайт полностью адаптивен для мобильных устройств:
- На планшетах и телефонах категории отображаются сверху
- Карточки выстраиваются в одну колонку
- Модальные окна занимают 95% экрана

## ⚡ Производительность

- **Lazy loading**: Полные документы загружаются только при открытии
- **Минимальные зависимости**: Только marked.js для рендеринга markdown
- **Нет фреймворков**: Чистый JavaScript для максимальной скорости

## 🐛 Известные ограничения

- Браузер должен поддерживать ES6+ (современные браузеры)
- Требуется JavaScript для работы сайта
- Файлы должны быть доступны по HTTP (не работает через `file://`)

## 📄 Лицензия

Документация GitLab EE - собственность GitLab Inc.
Сайт создан для удобного просмотра документации.

## 🤝 Контакты

Для вопросов и предложений создайте Issue в репозитории.

---

Made with 💻 in Terminal Style
